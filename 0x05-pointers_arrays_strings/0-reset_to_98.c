#include "main.h"

/**
 * reset_to_98 - takes a pointer to an int as parameter and updates the value it points to 98
 *
 * Return: Always 0
 */
int main(void)
{
	int n;

	n = 98;
	reset_to_98(n);
	return (0);
}
